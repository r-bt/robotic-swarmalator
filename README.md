# Distributed Swarmalator

This is python simulation of the swarmalator model running on distributed robots (i.e. each robot performs it's own computation and broadcast it's state over a lossly network)
